export const sendWelcomeEmail = async (email: string) => {
  // Stubbed email sender. In production, integrate with an email provider (e.g., Resend, SendGrid).
  console.log(`[emailService] Sending welcome email to ${email}`);
};
